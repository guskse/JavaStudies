# H2 LOCAL DATABASE -> http://localhost:8080/h2-console/

server:
  #set the port for your Spring Boot Application
  port:8080

spring:
  datasource:
    #we're using neon postgreSQL website for managing the url, username and password.
    url: jdbc:postgresql://ep-floral-morning-ac28s2ce-pooler.sa-east-1.aws.neon.tech/expensedb?sslmode=require
    driver-class-name: org.postgresql.Driver # the JDBC Driver for postgresql
    username: expensedb_owner #from neon console postgreSQL website
    password: npg_4Pvt6eOsiJxy #from neon console postgreSQL website
    platform: h2 #informs the database platform (h2, psql etc..)
  h2:
    console:
      enabled: true

  jpa:
    hibernate:
      ddl-auto: update #automatically create or updates the DB schema based on entity classes
    show-sql: true #Enables logging of SQL queries generated by JPA
    database-platform: org.hibernate.dialect.PostgreSQLDialect #translate the dialect (syntax) to specific db in use (postgres in this case)